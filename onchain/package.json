{
  "name": "finora-onchain",
  "version": "0.1.0",
  "private": true,
  "description": "Session-key smart wallet: spend limits, allowlist, owner kill switch, in-flight revocation.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:baseSepolia": "hardhat run scripts/deploy.ts --network baseSepolia",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "demo:attack": "hardhat run scripts/attackAgent.ts",
    "demo:redteam": "hardhat run scripts/redTeam.ts",
    "demo:peer": "hardhat run scripts/peerCredit.ts",
    "demo:live": "hardhat run scripts/proveSepolia.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10",
    "typescript": "^5.5.4"
  }
}
